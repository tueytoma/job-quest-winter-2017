import { reversePalette } from 'styled-theme/composer'

const theme = {}

theme.fonts = {
    primary: 'Kanit,sans-serif',
    pre: 'Kanit, Courier, monospace',
    quote: 'Kanit, Georgia, serif',
  }
  
  theme.sizes = {
    maxWidth: '1100px',
  }
  
  export default theme